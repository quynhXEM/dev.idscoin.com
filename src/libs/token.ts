

export const stake_Coin = async ({
    to,
    amount,
    chainId,
    tokenAddress,
    txHash
}) => {
    try {
        const 
    } catch (error) {
        return { ok : false, result: error?.toString()}
    }
}